{{#if @user.archived}}
  <div class="message is-warning">
    <div class="message-body p-1">
      This user is archived
    </div>
  </div>
{{/if}}

<button class="button" type="button" {{on "click" this.toggleArchived}}>
  {{if @user.archived "Unarchive" "Archive"}} user
</button>